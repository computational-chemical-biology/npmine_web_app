services:
  db:
    image: mcs07/postgres-rdkit:12-2020.03.2
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5434:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 5s
      retries: 5
      start_period: 10s
      timeout: 2s

  web:
    build: .
    environment:
      FLASK_ENV: ${FLASK_ENV:-development}
      FLASK_APP: websiteNPMINE:create_app()
      GUNICORN_CMD_ARGS: ${GUNICORN_CMD_ARGS:---reload --workers 1}
      SQLALCHEMY_DATABASE_URI: postgresql://${DB_USER}:${DB_PASSWORD}@db:5432/${DB_NAME}
      SECRET_KEY: ${SECRET_KEY}
      SQLALCHEMY_TRACK_MODIFICATIONS: ${SQLALCHEMY_TRACK_MODIFICATIONS}
      TEMP_FOLDER: ${TEMP_FOLDER}
      MAIL_SERVER: ${MAIL_SERVER}
      MAIL_PORT: ${MAIL_PORT}
      MAIL_USE_TLS: ${MAIL_USE_TLS}
      EMAIL_USER: ${EMAIL_USER}
      EMAIL_PASS: ${EMAIL_PASS}
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    command: >-
      bash -lc "conda run -n npmine_web_app flask db upgrade &&
      exec conda run -n npmine_web_app gunicorn $${GUNICORN_CMD_ARGS:-} --bind 0.0.0.0:5000 'websiteNPMINE:create_app()'"

volumes:
  postgres_data:
